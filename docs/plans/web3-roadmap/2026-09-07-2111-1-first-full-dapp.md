---
status: active
mission: web3-roadmap
work-item: M3-WI8
group: "2026-09-07-2111"
verify: [test, build, hardhat]
---

# 2026-09-07-2111-1-first-full-dapp 第一个完整 DApp（前端 + 合约交互）+ 文章

> Source: docs/backlog/web3-roadmap.md M3/WI8（第一个完整 DApp + 文章，对应 web3-roadmap-data.json M3.4）
> Related: 2026-09-07-2027-2-deploy-to-testnet（前置：M3.3 已 done，本计划在其 dapp/ 工程上加前端，承接其「合约就位，等一个前端：连钱包、读 count、点按钮调 increment」的 M3.4 预告结尾）；同源前置 2026-09-07-2027-1-hardhat-local-env（Counter 合约与测试出处）

## Current Baseline

- 依赖：前置计划 2026-09-07-2027-2（M3-WI7）已完成——`dapp/` 工程在位：`contracts/Counter.sol`（`count`/`owner` 状态变量、事件 `CountChanged(address indexed who, uint256 newValue)`、`increment`/`decrement`/`getCount`）、`test/counter.js` 4 用例全绿、`scripts/deploy.js`、`.env.example`（两占位符）、`hardhat.config.js`（solidity 0.8.20 + sepolia 配置 + dotenv quiet 加载）；M3.3 `done`。执行时若前置未落地，停止执行并上报依赖未满足。
- `src/data/web3-roadmap-data.json` 阶段 3：M3.1/M3.2/M3.3 均 `done`；M3.4 `todo`、`articleSlug: null`（本计划目标）；milestone 标题「第一个完整 DApp（前端 + 合约交互）」。
- `docs/backlog/web3-roadmap.md` Work Item Status 表 M3/WI8 行 Status `todo`，依赖 WI7（已 done，依赖链满足）。完成定义含：M3.4 置 done + **AGENTS.md 阶段 3 状态改 4/4**（AGENTS.md:107 现为 `1/4 (进行中)`——M3.2/M3.3 完成后按前两组计划 Non-Goals 约定未同步，留给本 WI 一次性收口）。
- `dapp/` **没有任何前端文件**（无 `frontend/` 目录、无 HTML/JS）；`dapp/package.json` 仅 `test: "hardhat test"` 一个 script，devDependencies：hardhat ^2.26、hardhat-toolbox ^5（自带 ethers v6 能力）、dotenv ^17。
- `dapp/hardhat.config.js` networks **只有 sepolia，没有 `localhost` 别名**——外部 `npx hardhat node`（127.0.0.1:8545，chainId 31337）+ `--network localhost` 联调需要本计划补上。
- 术语词典 `GlossaryTerm.astro` 已有：字节码/操作码/EVM/Gas/gwei/Solidity/Hardhat/账户模型/Nonce/智能合约/测试网/水龙头 等；**没有** `DApp`、`MetaMask` 词条——本篇文章的两个核心概念，按全局规范需先加词条再引用（ethers.js/RPC 等专有名词走行文不入词典，循 WI7 前例）。
- 已发布可内链 slug 共 11 篇 web3：utxo-model-deep-dive、pow-consensus、bitcoin-whitepaper-deep-dive、bitcoin-network-in-practice、account-model-vs-utxo、evm-deep-dive、ethereum-gas-fees、smart-contracts-explained、solidity-basics-notes、hardhat-local-env、deploy-to-testnet；deploy-to-testnet 结尾已留 M3.4 预告钩子（「连钱包、读 count、点按钮调 increment」「阶段 3 就闭环了」）。
- 已用 heroImage 11 张（photo-1554224155-6726b3ff858f、photo-1516245834210-c4c142787335、photo-1451187580459-43490279c0fa、photo-1554224154-26032ffc0d07、photo-1518770660439-4636190af475、photo-1518546305927-5a555bb7020d、photo-1639762681485-074b7f938ba0、photo-1555066931-4365d14bab8c、photo-1621761191319-c6fb62004040、photo-1581092160562-40aa08e78837、photo-1544197150-b99a580bb7a8），新图须第 12 张全库唯一。
- 密钥面：根 `.gitignore` 无锚定 `.env` 已覆盖 `dapp/.env`；roadmap 硬约束（助记词/私钥/RPC URL 只进 .env）针对真实密钥——`npx hardhat node` 打印的内置开发账户私钥是 Hardhat 文档公开的测试夹具、对应本地模拟网络账户，无任何真实资产，出现在联调脚本/文中不违反该约束（文中须注明这一点以免误导读者）。
- 验证门：`test`（npm run test:run）+ `build`（npm run build）+ `hardhat`（npm --prefix dapp test）；`type-check`/`lint` 存量失败不在门内（docs/testing/known-good-baselines.md）。
- 环境事实：Node v22.22.2 / npm 10.9.7 可用；`dapp/node_modules` 已安装；`docs/logs/2026/09-07.md` 已存在（当日日志追加即可，跨日按日志指南新建）。

## Goals

- `dapp/frontend/` 最小前端落地（单页原生 HTML/JS + ethers.js v6 公共 CDN UMD 构建）：连接 MetaMask（`window.ethereum` 检测 + `eth_requestAccounts`）、读合约状态（count）、发一笔写交易（`increment`）并等待确认 + `CountChanged` 事件回读，附状态/错误展示；合约地址经页面配置位填入本地部署输出。
- `dapp/hardhat.config.js` networks 追加 `localhost` 别名（http://127.0.0.1:8545）；默认 hardhat network 与 sepolia 行为不变，既有测试无回归。
- 本地联调通过（roadmap 完成门「`npx hardhat test` 与前端本地联调（localhost + hardhat network）通过」的可机械化形态）：后台 `npx hardhat node` → 部署到 localhost → `dapp/scripts/local-e2e.js` 冒烟脚本复刻前端数据流（provider 连 127.0.0.1:8545 → 读 count 基线 → Hardhat #0 公开夹具账户发 increment → 等 1 确认 → 复读 +1 → receipt 日志回读 CountChanged）exit 0；页面经本地 http 服务可访问且关键元素齐备；浏览器 + MetaMask 点击路径作为用户侧操作指引写入文章（如实注明脚本冒烟已验证同链路）。
- 词典新增 `DApp`、`MetaMask` 词条并先于文章使用。
- 发布文章 `first-full-dapp`（完整数据流：前端 → provider → 签名交易 → 打包 → 事件回读；串起阶段 1-3 全部概念：账户模型/nonce、gas、合约/EVM/字节码；结尾完成阶段 3 → 阶段 4 DeFi 过渡），150-250 行。
- 数据联动：M3.4 置 `done` + `articleSlug: "first-full-dapp"`；roadmap M3/WI8 行置 `done`；AGENTS.md 路线图阶段 3 行 `1/4 (进行中)` → `4/4`；当日日志记录条目。
- 验证门通过：`npm run test:run` exit 0；`npm run build` exit 0 且 `dist/blog/first-full-dapp/index.html` 生成；`npm --prefix dapp test` exit 0。

## Non-Goals

- 不做真实 Sepolia/主网上的前端联调——页面按 localhost + hardhat network 验证；切换 Sepolia 只是换合约地址与 MetaMask 网络，文中写清步骤即可，不作为完成门。
- 不引入 React/Astro island 工程、不做组件化/样式打磨/构建打包——roadmap 明文「单页即可，原生 HTML/JS + ethers.js CDN」，取最小实现。
- 前端交互取最小集（连接钱包 / 读状态 / increment 写交易 + 确认 + 事件回读）；不做 decrement 按钮、owner 展示、多网络切换 UI 等扩展交互。
- 不修改 `Counter.sol` 与既有测试用例（不动 M3.1/M3.2 产出；如联调暴露合约缺陷，停止并回报而非顺手改）。
- 不把 `dapp/frontend/` 纳入站点构建或部署（Cloudflare Pages 只发布站点 `dist/`，demo 页仅本地开发期使用）。
- 不启动阶段 4（DeFi）内容生产；不更新 AGENTS.md 阶段 3 以外的任何行。
- 不修复 `type-check` / `lint` 存量失败。

## Task Route

- Type: `implementation-only change`（dapp/ 增量：frontend/ 单页 + localhost network 别名 + e2e 冒烟脚本；一篇 MDX + 词典词条 + 数据联动含 AGENTS.md 收口）
- Owner Docs: `docs/backlog/web3-roadmap.md`（全局写作规范 + M3 续代码工程约定 + WI8 定义，含「AGENTS.md 阶段 3 状态改 4/4」与密钥条款）、`dapp/contracts/Counter.sol`（前端对接面来源，只读）
- Skill Selection Basis: 无匹配 skill——单页前端与内容写作为领域专项，无站点架构变更，同前七篇计划理由。

## Infrastructure And Config Prereqs

- Node v22.22.2 / npm 10.9.7（已核验在位）；`dapp/node_modules` 已装（hardhat 2.26 + toolbox 5 自带 ethers v6，冒烟脚本零新增依赖）。
- 联调本地跑 `npx hardhat node`（127.0.0.1:8545，chainId 31337）与一个静态文件服务（`python3 -m http.server` 或等价，仅开发期使用）。
- 浏览器打开页面时需公共 CDN 可达（ethers UMD）；站点 `npm run build` 与全部验证门不依赖 CDN。
- 密钥面：无真实密钥、无外部服务调用、无数据迁移；Hardhat #0 开发账户公开夹具私钥仅出现在联调脚本/文中说明（非真实资产，须注明）。

## Phase 1 — 前端单页与 localhost 网络

Targets: `dapp/frontend/index.html`, `dapp/frontend/config.js`, `dapp/hardhat.config.js`
Skill: none

- Item Types: `Add | Decision | Proof`
- Prereqs: 计划 2026-09-07-2027-2 已完成（dapp/ 工程 + M3.3 done，依赖链 WI7 → WI8 满足）

- [x] Decision: 前端形态选型——默认 roadmap 二选一中的「原生 HTML/JS + ethers.js CDN」：单页零构建，与 `dapp/` 独立工程约定一致，不把 demo 页并入站点生产构建。备选「Astro island」否决：引入站点构建耦合与 React 工程约束，超出 M3.4 最小集。残余风险：页面依赖 CDN 可达（离线不可用）——开发期可接受，文中注明。
- [x] Decision: 合约 ABI 承载——默认页面内嵌最小 human ABI（`increment`/`decrement`/`getCount`/`count`/`CountChanged` 事件）；备选「拷贝 artifacts JSON」否决：artifacts 是 gitignore 的构建产物且含字节码冗长不可稳定引用。一致性由本 Phase Proof 项核对。
- [x] Add: `dapp/frontend/config.js`——合约地址配置位（占位符 + 注释：填 `npx hardhat run scripts/deploy.js --network localhost` 输出的地址；换网络只改这一处）。
- [x] Add: `dapp/frontend/index.html`——单页：连接 MetaMask 按钮（`window.ethereum` 检测 + `eth_requestAccounts`，未装钱包给出明确提示）、读状态区（当前 count）、increment 按钮（构造写交易 → `tx.wait(1)` 等待确认 → 复读 count → 从 receipt/事件日志回读 `CountChanged(who, newValue)`）、状态/错误展示区；ethers.js v6 UMD 经公共 CDN 引入；无任何真实密钥。
- [x] Add: `dapp/hardhat.config.js` networks 追加 `localhost: { url: "http://127.0.0.1:8545" }`；默认 hardhat network 与 sepolia 配置不动。
- [x] Proof: 静态自检——本地 http 服务 `curl -s` 首页 exit 0 且 HTML 含 ethers CDN 引用与连接/读/increment 关键元素；页面内嵌 ABI 与 `Counter.sol` 函数/事件签名逐项一致（increment()/decrement()/getCount()/count/CountChanged(address,uint256)）；`grep -nE "0x[0-9a-fA-F]{32,}" dapp/frontend/` 无真实私钥形态字符串；`npm --prefix dapp test` 仍 exit 0（配置增量无回归）。

Exit Criteria:

- [x] `dapp/frontend/` 单页落地，静态可服务，连接钱包/读状态/写交易/等待确认/事件回读五个交互面齐备。
- [x] localhost network 别名生效且既有配置无回归；密钥面干净。
- [x] 两项 Decision（形态、ABI 承载）记录在案，含备选与否决理由。

## Phase 2 — 本地联调与冒烟验证

Targets: `dapp/scripts/local-e2e.js`
Skill: none

- Item Types: `Add | Proof`
- Prereqs: Phase 1

- [x] Add: 冒烟脚本 `dapp/scripts/local-e2e.js`（参数或环境接收合约地址）——复刻前端数据流：`JsonRpcProvider` 连 http://127.0.0.1:8545 → 实例化 Counter → 读 count 基线 → 以 Hardhat #0 开发账户公开夹具私钥签名 `increment` → 等 1 个确认 → 复读 count 为基线+1 → 从 receipt 日志回读 `CountChanged`（who == 签名账户、newValue == 新值）→ 全部断言通过 exit 0，任一步失败非零退出并打印差异。
- [x] Proof: 联调序列实跑——后台起 `npx hardhat node` → `npx hardhat run scripts/deploy.js --network localhost` 记录合约地址 → `node scripts/local-e2e.js <地址>` exit 0 → 结束后关闭节点进程（不留孤儿进程）；关键输出（部署地址、交易 hash、count 变化、事件参数）粘贴进执行记录。

Exit Criteria:

- [x] 读 → 写 → 确认 → 事件回读全链路在 localhost + hardhat network 上脚本验证通过（exit 0）。
- [x] `npm --prefix dapp test` 无回归（Phase 1 已核一次，此处收口前最后一次全绿复核见 Phase 4）。
- [x] 联调步骤可复现，无孤儿进程残留。

## Phase 3 — 词典与文章写作

Targets: `src/components/web3/GlossaryTerm.astro`, `src/content/blog/first-full-dapp.mdx`
Skill: none

- Item Types: `Add | Decision | Proof`
- Prereqs: Phase 2

- [x] Add: `GlossaryTerm.astro` definitions 追加两个 key：`DApp`（去中心化应用：前端界面 + 链上智能合约作后端，规则上链、无人可单方面下架或篡改——呼应阶段 3 的平台封号痛点）与 `MetaMask`（主流浏览器加密钱包：管理账户与私钥、为交易签名，并向网页注入 window.ethereum 供 DApp 连接，是普通用户进入以太坊生态最常用的入口）。风格与既有词条一致，不与 智能合约/账户模型/测试网 词条口径冲突。
- [x] Add: 创建 `src/content/blog/first-full-dapp.mdx`，frontmatter：`title`、`categories: ["Web3"]`、`subject: "DApp"`、`tags: ["Web3", "区块链入门", "DApp"]`、`pubDate` 执行当天、`description` 摘要、`heroImage` 为 Unsplash 图（`?w=1200&h=630&fit=crop` 后缀）。
- [x] Decision: 主题 tag 取 `DApp`——阶段 3 收官篇的总括性概念（同组前例 Hardhat/Sepolia 均取篇名核心词）；备选 `MetaMask` 否决：工具名不及概念名总括。heroImage 选图——`grep -h "heroImage" src/content/blog/*.mdx` 全库比对确认第 12 张唯一后选定（避开 Current Baseline 已列 11 图）。备选与残余风险记录在案（无）。
- [x] Add: 正文 150-250 行：引言（承接 deploy-to-testnet 结尾「合约就位，等一个前端」，回到阶段 3 painPoint——规则上链的应用终于可点）→ 前端三件套分节（连接钱包 / 读状态 / 写交易 + 等待确认 + 事件回读，贴 `dapp/frontend/` 真实代码）→ 完整数据流 ASCII 图与讲解（页面 → provider → 签名交易 → 内存池/打包执行 → 事件回读，每步标注对应阶段 1-3 概念：账户模型/nonce、gas、合约字节码/EVM）→ 本地联调步骤分节（`npx hardhat node` + 本地 http 服务 + MetaMask 添加 localhost:8545（chainId 31337）+ 导入 Hardhat #0 账户 + 填合约地址；密钥安全提醒：#0 是公开测试夹具，真实私钥永不入代码）→ 总结：阶段 3 闭环（从「理解」到「动手写」），过渡阶段 4（DeFi：把这样的 DApp 组合成的金融市场）。
- [x] Add: 组件使用合规——`<Highlight>` 仅四种 type（密钥安全用 `type="danger"`）；`<GlossaryTerm>` 仅引用词典已有 key（含新增 `DApp`、`MetaMask`，大小写完全一致）；禁止 `client:` 指令；图示全 ASCII `text` 代码块。
- [x] Add: 文末「相关文章」内链仅引用已发布 slug（11 篇 web3 中精选，slug 必须真实存在；deploy-to-testnet 与 hardhat-local-env 为必引前篇）。
- [x] Proof: `wc -l` 打印值 150-250；每个 `GlossaryTerm term="X"` 词典命中且大小写一致；heroImage 唯一性复查通过；文中代码/命令与 `dapp/` 实际文件逐项一致（文件名、函数签名、事件名、端口、chainId）。
- [x] Proof: 浏览器用户侧路径与文中步骤一致性核对——文章联调分节的命令/端口/chainId（31337）/#0 账户导入说明与 `dapp/` 实况逐项一致；文中如实注明「脚本冒烟已在 localhost + hardhat network 验证通过（Phase 2），浏览器 + MetaMask 为用户侧操作指引」。

Exit Criteria:

- [x] 词典含 `DApp`、`MetaMask` key，先于文章使用落地；文章文件存在，frontmatter 全字段合规，行数 150-250。
- [x] 前端三件套 / 完整数据流 / 本地联调步骤 / 总结过渡四个内容块齐备，数据流串起阶段 1-3 概念，结尾完成阶段 3 → 4 过渡。
- [x] 组件与内链合规；heroImage 全库唯一；无真实密钥；文章联调描述与工程实况一致且如实注明脚本冒烟已验证、浏览器路径为用户侧操作指引。

## Phase 4 — 数据联动与验证（含 AGENTS.md 收口）

Targets: `src/data/web3-roadmap-data.json`, `docs/backlog/web3-roadmap.md`, `AGENTS.md`, `docs/logs/2026/`
Skill: none

- Item Types: `Add | Proof`
- Prereqs: Phase 3

- [x] Add: `web3-roadmap-data.json` 阶段 3 的 M3.4 `status: "todo"` → `"done"`、`articleSlug: null` → `"first-full-dapp"`；其余 milestone 不动（M3.1-M3.3 保持 done，阶段 2/4/5 全不动）。
- [x] Add: `docs/backlog/web3-roadmap.md` Work Item Status 表 M3/WI8 行 Status `todo` → `done`。
- [x] Add: `AGENTS.md` 路线图表格阶段 3 行 `1/4 (进行中)` → `4/4`——roadmap WI8 完成定义显式授权的唯一 AGENTS.md 同步动作；其余行不动。
- [x] Add: `docs/logs/2026/` 当日日志条目（追加至既有当日文件；跨日则按日志指南新建）。
- [x] Proof: `npm run test:run` exit 0。
- [x] Proof: `npm run build` exit 0 且 `test -f dist/blog/first-full-dapp/index.html` 为真（同时证明 `dapp/frontend/` 不影响站点构建）。
- [x] Proof: `npm --prefix dapp test` exit 0（收口前最后一次全绿复核）。

Exit Criteria:

- [x] M3.4 done + articleSlug 正确；roadmap WI8 行 done；AGENTS.md 阶段 3 行 4/4 且无其他改动；日志条目在档。
- [x] `test` / `build` / `hardhat` 三验证键均 exit 0，新文章路由生成。
- [x] `docs/logs/` 更新（本计划闭环条目）。

## Draft Review Record

- dispatch review #review-2026-09-07-202508-mission-driver-2026-09-07-2111-1-first-full-dapp-1-60f0ccfa to ses-opencode-glm53
- 2026-09-07：iteration 1，共识 acceptable-after-fixes #review-2026-09-07-202508-mission-driver-2026-09-07-2111-1-first-full-dapp-1-60f0ccfa

## Verification

- pass test 2026-09-07-202508-mission-driver exit=0
- pass build 2026-09-07-202508-mission-driver exit=0
- pass hardhat 2026-09-07-202508-mission-driver exit=0

## Closure

- dispatch audit #audit-2026-09-07-202508-mission-driver-2026-09-07-2111-1-first-full-dapp-1-86675200 to ses-opencode-glm53 models={exec:opencode-glm53,aud:opencode-glm53}
- accepted #audit-2026-09-07-202508-mission-driver-2026-09-07-2111-1-first-full-dapp-1-86675200：审计通过——四阶段产物全部落地核验（dapp/frontend/ 两文件 + localhost 别名、local-e2e.js 冒烟、词典 DApp/MetaMask 2 key + 198 行文章、M3.4/roadmap WI8/AGENTS.md 阶段 3 行 4/4 数据联动、日志条目在档）；三个验证键由审计会话实跑复核全绿（npm run test:run exit=0 4/4、npm run build exit=0 592 pages 且 dist/blog/first-full-dapp/index.html 存在、npm --prefix dapp test exit=0 4 passing）；exec 与 aud 同为 opencode-glm53，如实记录为单模型降级通道
