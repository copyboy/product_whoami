---
status: active
mission: web3-roadmap
work-item: M2-WI2
group: "2026-09-07-1319"
verify: [test, build]
---

# 2026-09-07-1319-2-evm-deep-dive 文章「EVM 是什么，为什么重要」

> Source: docs/backlog/web3-roadmap.md M2/WI2（文章「EVM 是什么，为什么重要」，对应 web3-roadmap-data.json M2.2）
> Related: 2026-09-07-1319-1-account-model-vs-utxo（前置：WI1，先落地）、2026-09-07-1319-3-ethereum-gas-fees（后继：WI3 依赖本计划）

## Current Baseline

- 目标输出文件 `src/content/blog/evm-deep-dive.mdx` 不存在；Web3 系列已发布 4 篇 Bitcoin 文章（`utxo-model-deep-dive`、`pow-consensus`、`bitcoin-whitepaper-deep-dive`、`bitcoin-network-in-practice`）。
- `src/data/web3-roadmap-data.json` 阶段 2 M2.2 为 `status: "todo"`、`articleSlug: null`。
- `docs/backlog/web3-roadmap.md` Work Item Status 表 M2/WI2 行 Status 为 `todo`，依赖 WI1。
- 术语词典 `GlossaryTerm.astro` 已有 `EVM`（图灵完备虚拟机 + 生态护城河表述）、`Layer2`、`Rollup`、`Optimistic Rollups`、`ZK Rollups`、`Ethereum`、`共识机制`、`PoS`、`Gas` 等 key；**没有** `字节码`、`操作码` 词条。
- `src/pages/web3/report.astro` 已有 EVM 兼容生态表述可呼应（L2 表格含 Arbitrum/Base，Gas 口径见第 287 行附近「以太坊主网 Gas 通常在个位数到几十 gwei 波动……L2 不足 0.01 gwei」）。
- 前篇 `utxo-model-deep-dive.mdx` 确立的写作模式与 `Highlight`（仅 info/warning/success/danger）、ASCII 代码块、150-250 行、frontmatter 规范同 WI1 计划 Phase 2 所载，此处不重复展开。
- 执行时前置状态（由计划 1 落地保证）：`account-model-vs-utxo` 已发布、M2.1 done——本篇「相关文章」可内链这 5 个真实 slug（4 篇 Bitcoin + 账户模型篇）。
- 验证门与存量失败口径同 WI1 计划：`build` + `test:run` 为门，`type-check`/`lint` 存量失败不在门内（docs/testing/known-good-baselines.md）。

## Goals

- 发布文章 `evm-deep-dive`：EVM 作为确定性沙盒虚拟机（同样输入必得同样输出，是共识的前提）；字节码、操作码与执行模型（栈式机，1024 栈深上限提一句即可）；「一次编写多链运行」的 EVM 兼容生态（Arbitrum/Base/Optimism，呼应 report 页）；网络性护城河论点；结尾过渡引出 Gas（执行计算如何定价）。
- 词典新增 `字节码`、`操作码` 词条并在文中使用。
- 数据联动：M2.2 置 `done` + `articleSlug: "evm-deep-dive"`；roadmap M2/WI2 行置 `done`；当日日志记录条目。
- 验证门通过：`npm run build` exit 0 且 `dist/blog/evm-deep-dive/index.html` 生成；`npm run test:run` exit 0。

## Non-Goals

- 不写 M2.3/M2.4 文章（M2.3 见 Related 后继计划；M2.4 后续轮次起草）。
- 不更新 `AGENTS.md` 阶段 2 状态列（仅 M2.4/WI4 完成时更新）。
- 不修复 `type-check` / `lint` 存量失败。
- 不改 `src/pages/web3/report.astro`——口径呼应是单向引用（文章对齐 report 页既有数字），不反向修改 report 页。

## Task Route

- Type: `implementation-only change`（纯内容新增：一篇 MDX + 词典词条 + 数据联动）
- Owner Docs: `docs/backlog/web3-roadmap.md`（全局写作规范 + WI2 定义）、`src/pages/web3/report.astro`（EVM 兼容生态与费用数字的口径来源，只读）
- Skill Selection Basis: 无匹配 skill——同 WI1 计划理由。

## Infrastructure And Config Prereqs

- Node 环境且 `node_modules/` 已安装；无其他 infra 依赖；无数据迁移。

## Phase 1 — 词典与口径准备

Targets: `src/components/web3/GlossaryTerm.astro`（修改）；`src/pages/web3/report.astro`（只读，口径来源，不修改）
Skill: none

- Item Types: `Add | Proof`
- Prereqs: 计划 2026-09-07-1319-1 已完成（M2.1 done，依赖链 WI1 → WI2）

- [x] Add: 在 `GlossaryTerm.astro` 的 `definitions` 追加两个 key——`字节码`（智能合约编译后的 EVM 指令序列，部署上链、由 EVM 逐条执行，人不可直接读）与 `操作码`（EVM 的最小指令集单元，栈式执行；与字节码的关系：字节码 = 操作码序列）。风格与既有词条一致。
- [x] Proof: `grep -n "字节码\|操作码" src/components/web3/GlossaryTerm.astro` 打印两个新 key 定义行；重读 `report.astro` 的 L2 生态与 Gas 数字段（Arbitrum/Base 表格 + 关键数据行），记录本篇需对齐的口径（主网个位数到几十 gwei、L2 不足 0.01 gwei）。

Exit Criteria:

- [x] 词典含 `字节码`、`操作码` 两个 key，先于文章使用落地。
- [x] report 页口径清单（EVM 兼容链名单 + 费用数字）已核对并作为写作输入。

## Phase 2 — 文章写作

Targets: `src/content/blog/evm-deep-dive.mdx`
Skill: none

- Item Types: `Add | Decision | Proof`
- Prereqs: Phase 1

- [x] Add: 创建 `src/content/blog/evm-deep-dive.mdx`，frontmatter：`categories: ["Web3"]`、`subject: "Ethereum"`、`tags: ["Web3", "区块链入门", "Ethereum", "EVM"]`、`pubDate` 执行当天、`description` 摘要、`heroImage` 为 Unsplash 图（`?w=1200&h=630&fit=crop`）。
- [x] Decision: heroImage 选图——`grep -h "heroImage" src/content/blog/*.mdx` 比对确认与现有全部文章不重复后选定（主题任选，唯一性是硬约束）。残余风险：无（可机械复查）。
- [x] Add: 正文 150-250 行：引言（承接账户模型篇结尾的「状态由谁来执行」之问）→ 确定性沙盒（同样输入必得同样输出 = 共识前提；与「世界计算机」定位的关系）→ 执行模型分节（源码 → 字节码 → 操作码逐条执行；栈式机一句话讲清，1024 栈深上限一句带过；ASCII text 代码块画「合约调用 → EVM 执行 → 状态改写」示意）→ EVM 兼容生态（一次编写多链运行：Arbitrum/Base/Optimism，呼应 report 页）→ 网络性护城河论点 → 总结过渡：执行每一步都要计价，引出 Gas。
- [x] Add: 组件使用合规——`<Highlight>` 仅四种 type；`<GlossaryTerm>` 仅引用词典已有 key（含 Phase 1 新增的 `字节码`、`操作码`，以及已有的 `EVM`/`Layer2`/`Rollup` 等）；无 `client:` 指令；图示全 ASCII。数字用「量级/约」表述，与 report 页口径一致。
- [x] Add: 文末「相关文章」内链仅引用已发布 slug：4 篇 Bitcoin 文章 + `account-model-vs-utxo`（前置计划已保证其存在；执行时若前置未落地，停止执行并上报依赖未满足，不得引用不存在的 slug）。
- [x] Proof: `wc -l` 打印值 150-250；每个 `GlossaryTerm term="X"` 在词典中命中且大小写一致；heroImage 唯一性复查通过。

Exit Criteria:

- [x] 文章文件存在，frontmatter 全字段合规，行数 150-250。
- [x] 确定性沙盒 / 执行模型 / 兼容生态 / 护城河四个论点齐备，结尾完成向 Gas 的过渡。
- [x] 组件与内链合规；report 页口径对齐。
- [x] heroImage 全库唯一。

## Phase 3 — 数据联动与验证

Targets: `src/data/web3-roadmap-data.json`, `docs/backlog/web3-roadmap.md`, `docs/logs/2026/`
Skill: none

- Item Types: `Add | Proof`
- Prereqs: Phase 2

- [x] Add: `web3-roadmap-data.json` 阶段 2 的 M2.2 `status: "todo"` → `"done"`、`articleSlug: null` → `"evm-deep-dive"`；其余 milestone 不动。
- [x] Add: `docs/backlog/web3-roadmap.md` Work Item Status 表 M2/WI2 行 Status `todo` → `done`。
- [x] Add: `docs/logs/2026/` 当日日志条目（追加或按日志指南新建当日文件）。
- [x] Proof: `npm run test:run` exit 0。
- [x] Proof: `npm run build` exit 0 且 `test -f dist/blog/evm-deep-dive/index.html` 为真。

Exit Criteria:

- [x] M2.2 done + articleSlug 正确；roadmap WI 行 done；日志条目在档。
- [x] `npm run test:run` 与 `npm run build` 均 exit 0，新文章路由生成。
- [x] `docs/logs/` 更新（本计划闭环条目）。

## Draft Review Record

- dispatch review #review-2026-09-07-131958-mission-driver-2026-09-07-1319-2-evm-deep-dive-1-c6538cdd to ses_opencode_reviewer
- 2026-09-07：iteration 1，共识 acceptable-after-fixes #review-2026-09-07-131958-mission-driver-2026-09-07-1319-2-evm-deep-dive-1-c6538cdd

## Verification

- pass test 2026-09-07-131958-mission-driver exit=0
- pass build 2026-09-07-131958-mission-driver exit=0

## Closure

- dispatch audit #audit-2026-09-07-131958-mission-driver-2026-09-07-1319-2-evm-deep-dive-1-0f638e24 to ses_opencode_auditor models={exec:opencode-glm,aud:opencode-glm}
- accepted #audit-2026-09-07-131958-mission-driver-2026-09-07-1319-2-evm-deep-dive-1-0f638e24：审计通过——文章 `src/content/blog/evm-deep-dive.mdx`（157 行，150-250 区间）已发布且 heroImage 全库唯一（grep 复查 1 hit）；词典 `字节码`/`操作码` 已落地（GlossaryTerm.astro:20-21）且文中 GlossaryTerm 引用全部命中词典既有 key；M2.2 done + articleSlug=`evm-deep-dive`、roadmap M2/WI2 行 done、docs/logs/2026/09-07.md 条目在档；实测验证门 `npm run test:run` exit=0（4 tests passed）与 `npm run build` exit=0（572 pages，`test -f dist/blog/evm-deep-dive/index.html` 为真）。exec/aud 同模型为声明的单模型降级，如实记录。
