# 复制本文件为 dapp/.env 后填入真实值（.env 已被 .gitignore 覆盖，永不入库）

# Sepolia RPC URL —— 在 RPC 节点服务（Alchemy / Infura 等）注册并创建 App 后，
# 复制其提供的 Sepolia HTTPS 端点（形如 https://eth-sepolia.g.alchemy.com/v2/<KEY>）
SEPOLIA_RPC_URL=

# 部署账户私钥 —— 用「测试专用」新账户的私钥（0x 开头），
# 只从 .env 读取；永不提交 git、永不发给任何人
PRIVATE_KEY=
